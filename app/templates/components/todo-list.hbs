{{#each items key="@guid" as |item|}}
  {{todo-list-item item=item on-change=(action "updateItem")
         on-delete=(action "deleteItem") show-if-completed=showIfCompleted
         show-if-uncompleted=showIfUncompleted}}
{{/each}}
