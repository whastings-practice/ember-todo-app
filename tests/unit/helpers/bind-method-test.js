import { bindMethod } from '../../../helpers/bind-method';
import { module, test } from 'qunit';

module('Unit | Helper | bind method');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bindMethod(42);
  assert.ok(result);
});
